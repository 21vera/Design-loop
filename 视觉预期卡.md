# 视觉预期卡

用于 PM 与设计在出稿前快速对齐视觉预期。

视觉预期卡没有默认题目。每次都要先理解具体需求，再拆出这次真正影响视觉判断的问题。

## 流程

### 1. 输入具体需求

PM 提供功能背景、所在页面和希望新增或调整的内容。

产品目标、用户范围和功能逻辑默认已经明确，视觉预期卡不重复讨论这些内容。

### 2. 根据需求拆问题

设计只找出本次最关键的视觉分歧点，并生成 2–3 道选择题。

可能涉及的判断包括：

- 要多显眼
- 看起来像什么
- 不能像什么
- 要不要标题（适用于新增 Banner、通知气泡等信息模块）
- 第一眼先看到什么
- 与同屏内容谁轻谁重
- 要增强的是被看见、被看懂还是被点击

以上只是拆题方向，不是固定题目。每次只选择与当前需求有关的部分。

问题要求：

- 使用简单口语
- 每题提供 A、B、C 选项
- 选项之间有明确差别
- 答案会直接影响视觉方案
- 不询问具体的颜色、字号、图标或组件形式
- 不超过 3 题

PM 只需回复编号和选项，例如：`1A 2B 3C`。

### 3. 输出视觉预期

收到答案后，设计将答案合并成一段明确的视觉预期。

输出要求：

- 只输出结论，不复述问卷
- 控制在 1–2 句话
- 说明显眼程度、视觉角色和需要避免的感受
- 必要时说明第一眼要强调的信息
- 不展开解释，不提前输出完整设计方案

## 示例：提醒用户开启 Chatbot

具体需求：在 Chat 工作台增加一个模块，提醒用户开启 Chatbot。

根据这个需求，拆出的问题：

```text
1. 多显眼？
   A. 一进来必须看到
   B. 顺手能看到
   C. 能找到就行

2. 像什么？
   A. 待处理提醒
   B. 小助手提示
   C. 新功能推荐

3. 不能像什么？
   A. 广告
   B. 弹窗
   C. 普通说明文字
```

PM 回答：`1A 2A 3C`

视觉预期：

> 首屏要有一个明确的“开启 Chatbot”待办提醒；它比普通说明文字更醒目，有标题和行动按钮，但整体像系统任务，不做成营销广告或大幅宣传卡。

## 核心原则

不是用一套固定问题套所有需求，而是针对具体需求，找到最影响这次设计落地的视觉判断，再把这些判断变成 PM 可以快速回答的选择题。
