# Design Loop 包内执行规则

- 用户要求安装、初次打开或检查本包时，先完整读取并执行 `00_总流程/setup-design-loop/SKILL.md`。
- Step 0 未达到完成标准时，不进入具体 Case。
- Step 0 的安装、工具发现、插件或 MCP 配置、连接发起、浏览器打开、授权后复检均由 AI 自动完成。不得把可由 AI 执行的安装或配置步骤转交给用户；只有登录、OAuth 同意、权限批准、验证码、安全密钥等必须由本人完成的动作才请求用户操作，并且每次只请求一个动作。
- 用户开始具体需求时，按 `00_总流程/Design Loop总流程与调用时机.md` 执行。
- `04_Case归档模板` 只作为源模板保留在本包内。真实 Case 必须由 AI 复制到桌面独立目录 `Design Loop Cases/<Case名称>/`；不得把真实 Case、过程稿、截图、研究结果或最终交付写回 Design Loop 源包。
- 每一步进入下一步前，AI 按本步输出标准自检；未达到就在本步继续循环。
- 每一步开始前，必须按总流程中的“强制调用”逐项调用；不得因已熟悉内容、已有上下文或能力相似而跳过。任一必需项未成功调用，本步不得完成，也不得进入下一步。
- 每次生成、编辑、合成或更新设计图片及视觉稿前，必须通过 `00_总流程/Design Loop总流程与调用时机.md` 的「设计规范门禁」：完整读取 L1 原子变量与 Figma `✅ Design Token 设计令牌` 页（`Seller Center Library` node `1:2`），再按需读取组件层与页面层。**Token 层每轮重新读取，不允许降级、不允许凭记忆、不允许沿用上一轮结果；未完成读取不得出图。** 组件与页面文档可能落后于 library，按 L5「未建组件降级规则」处理并登记；Token 层不适用降级。
- 交付任何设计稿前，必须先自行完成规范自检（Token 清单比对 + L5 检查项 + 结构健壮性 + 页面完整性）。未全部通过就回到出图步骤修改并重新自检，持续循环，不得交付、不得标注"基本符合"、不得把未通过项留给用户判断。同一项连续三轮无法通过时才停下来说明卡点。
- Product Design 是 Codex 环境的插件，按 **Step 0 探测结果**决定是否强制，不靠判断"当前是不是 Codex"。Step 0 逐个探测 `ideate` / `image-to-code` / `audit` 并把结果写进 Case 标记，各自独立判定、独立降级。
- **标记为可用的工作流是硬门槛**：对应环节每次出图都必须成功调用，只读取 `product-design:index` 不算成功调用（方向探索用 `ideate`，Figma 原稿转 HTML 基线用 `image-to-code`，检查现有方案用 `audit`）。调用失败时停止本次出图并提示：`Product Design 插件未成功调用，请 @Product Design 后让我继续本次出图。` 不得静默改用其他能力出图。
- **标记为不可用的工作流是软门槛**：对应环节跳过，改用 Figma 读写 + Shopee 设计系统正常出图，在 `_自检回执.md` 记一笔，不阻塞流程也不反复重试。三个全部不可用时视觉阶段照常运行。
- 粗方案 HTML 与细方案 Figma 都必须以用户指定的节点级 Figma 原稿为唯一视觉基线。先复制或高还原原稿，再只改动本次方案必需区域；未改区域不得重新设计、简化、换壳或改写内容。
- 每次粗方案或细方案生成、修改后，必须按 `00_总流程/QA执行方式.md` 的「Figma 原稿超高还原门禁」在同一视口、同一状态执行原稿与产出的并排、叠加或像素差异比对。未达到门禁就继续修改并重检，不得交付。
- Step 2 粗方案默认使用 Figma 原图高清底板作为未改区域，只在变化区域构建真实交互层；底板区域不做像素比对也不做 Token 核验，自绘区域严格 QA。细方案复制原 Figma Frame 与组件实例后做最小改动，不用底板。
- 底板区域虽不做样式像素比对，但必须参加整屏内容完整性 QA。底板或任一关键状态中的非设计意图空白商品图、缺图、错图、断图、低清和错误裁切均不得通过；先从 Figma 当前节点、相邻状态、组件实例或页面已有图片恢复，仍没有时按标题和画面其他图片选择合适近似图并实际填入后重检。
- 粗方案 HTML 固定使用 `build-rough-prototypes` 规定的简洁评审画板：顶部只放 Case 名称和 A/B/C；左栏只放方案身份与 1 句核心机制、2 条关键优点、1 条主要缺点、两行推荐理由（仅唯一推荐方案）、主流程、1 句体验说明与重置、3 条纯文字边界情况。锁定的桌面评审视口中左栏全部信息必须无需滚动即可看完。右侧只放当前步骤和产品画面，页面之外只允许一层浅色承托底，不得再套深灰画布、第二层背景、卡片容器或厚重阴影。全部步骤保持同一壳层和位置。画板不得显示“DESIGN LOOP · 粗方案”“Figma 原图基线”“1440 × 900”“还原策略”“粗方案仅标注”“原图底板”“自绘层”“命中层”“像素比对”“QA 通过”“Agent Reach”“Mobbin”“Token”等实现、研究过程或 QA 元信息。
- 用户完成视觉预期选择后，AI 用 1–2 句话复述并直接锁定，随后立即进入 Step 3B；不得要求用户再回复一次“确认”。用户有异议时可随时打断，收到打断后停止当前执行并按最新反馈修正。
- 选定粗方案进入 Step 3B 后，粗方案只作为交互机制、主流程、信息层级和业务规则来源；视觉以原 Figma、设计系统和已锁定视觉预期为准。Product Design 只做进入 Figma 前的内部承载检查，不额外制作、精修或 QA 一张中间视觉图；调用成功后直接复制原 Frame，在 Figma 中原生搭建并以 Figma 产出执行高标准 QA。
- Step 3B 进入检查只核对选定方案、已锁定视觉预期、原 Figma 节点和 Case 交付目录；四项齐全即开始，不增加二次确认，不得重新生成或精修粗方案、重复 Step 2 QA，或把方案归档扩展成出图任务。同一次连续 Step 3B 的设计规范门禁只读取一次，能力切换不重复读取；进入新的 Step 4 反馈轮次再重新读取。
- 底板必须高清：导出倍率 ≥ 2×（`maxDimension` 至少为目标视口宽度的 2 倍，不得用默认值），只等比缩小不放大，不二次压缩，100% 查看时文字与图标全部清晰可辨，导出后必须实际查看确认。底板发虚即判不通过并重新导出。
- 还原 QA 的红线是「不能有一眼就看出来的问题」：元素重叠遮挡、错位、文字溢出、断图、比例失真、布局重排、底板与自绘层接缝，出现任一条即不通过且不受像素容差保护；自绘区域量化容差 ≤ 2px。
- 粗方案只强制实现并 QA 主流程。失败、加载、权限、数据变化、中断恢复等边界流程必须完成内部定义，并在画板中用简短中文文字标注；除非边界本身决定方案是否成立，否则不要求做成可点击交互。
- 面向用户的分析、方案说明、研究结论、画板注释、推荐理由和下一步默认使用简体中文。页面内面向东南亚买家或卖家的产品文案继续按 Step 1 已确认的目标市场与界面语言输出，不因分析语言改为中文。
