# 环境要求

拿到这个包之后，先看这一页。下面分成「AI 能自己搞定的」和「必须你本人装的」两类——后者 AI 代劳不了，涉及系统级安装、GUI 弹窗、账号登录或付费订阅。

## 一、必须你本人准备（AI 代劳不了）

| 项 | 用途 | 怎么弄 | 缺了会怎样 |
|---|---|---|---|
| **Figma 账号 + 目标文件访问权** | 读设计规范、读原稿、写原型 | 登录 Figma，确认能打开 `Seller Center Library` | **整个流程不可用** |
| **Figma MCP / 插件连接** | AI 读写 Figma | 在你的 AI 工具里连接 Figma，完成 OAuth | **整个流程不可用** |
| **Product Design 插件** | 每次出图前的强制调用 | 在你的 AI 平台安装并启用 | **视觉阶段不可用** |
| **Python ≥ 3.10** | Agent Reach 的运行前置 | 见下方说明 | 市场调研降级 |
| **Mobbin 账号（含订阅）** | 竞品 screen / flow 检索 | 登录并连接 Mobbin MCP | 市场调研降级 |

### 关于 Python

macOS **默认不带**可用的 Python。`/usr/bin/python3` 只是一个占位符，调用它只会弹 Xcode 安装框。

先检查：

```bash
python3 --version
```

输出 ≥ 3.10 就够了。不够或报错，三选一：

```bash
# 推荐：Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python pipx && pipx ensurepath
```

```bash
# 或：官方安装包 https://www.python.org/downloads/macos/
```

```bash
# 或：Xcode Command Line Tools（会弹 GUI，可能要管理员密码）
xcode-select --install
```

> 第三种最省事，但它自带的 Python 通常是 3.9.x，**可能不够**。装完先跑 `python3 --version` 确认。

装好 Python 后，Agent Reach 从包内离线源安装，不需要联网拉 GitHub：

```bash
pipx install ./02_交互/agent-reach/runtime
agent-reach doctor --json
```

详见 [`02_交互/agent-reach/安装说明.md`](02_交互/agent-reach/安装说明.md)。

## 二、AI 会自己搞定

- 安装包内全部 Skill 到你的 Skill 目录
- 发现、配置、连接可自动化的 MCP 与插件
- 打开登录/授权页面到可操作状态（你只需要点确认）
- 失败诊断与安全重试
- 建立桌面 Case 工作区

## 三、降级规则

不是所有东西缺了都会卡死。分两类：

**硬门槛——缺了不能开始**：Figma 连接、Product Design 三个工作流（`ideate` / `image-to-code` / `audit`）、能读到 Design Token 页、本地 HTML 预览能力。

**软门槛——缺了可以继续，但 AI 必须如实告知并记录降级**：Mobbin、Agent Reach 及其各 channel。

AI 不得把降级说成就绪，也不得因为工具缺失就编造竞品行为、来源或数据。

## 四、这个包不包含什么

- **不包含 Figma 文件本身**。所有设计规范的真源是 `Seller Center Library`（fileKey `KTvcllui6OGgNPI5f1DfHe`）。换一个 Shopee 之外的项目，需要替换 `03_视觉/Shopee设计系统` 整层。
- **不包含 Product Design 插件的安装源**。包里没有它的下载地址，需要你在自己的 AI 平台上找到并安装。
- **不包含任何真实 Case**。`04_Case归档模板` 是只读源模板，真实 Case 由 AI 复制到桌面 `Design Loop Cases/`。

## 五、怎么开始

把这个文件夹作为工作区打开，然后说：

> 请安装这个 Design Loop 包。

AI 会执行 [`00_总流程/setup-design-loop/SKILL.md`](00_总流程/setup-design-loop/SKILL.md)，逐项检查并告诉你还缺什么。

装完后开始需求时，**必须同时给一个节点级 Figma 链接**（带 `node-id=` 的那种）——改造现有产品给目标页面，全新项目给设计系统页或参考页。后续所有出图都以它为视觉基线，没有它整条流程走不下去。
