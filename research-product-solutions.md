---
name: research-product-solutions
description: 基于产品需求，使用 Mobbin 与 Agent Reach 调研已有产品如何解决同类或相邻问题，归纳现有方案类型、适用条件、优缺点、可借鉴点与不建议照抄之处，并向后续设计流程输出有证据的方向性约束。用于“先看看市面上怎么做”“调研同类产品方案”“给后续设计提供竞品依据”等请求；只负责研究，不负责产品立项、需求定义、界面方案、交互稿、原型、Figma 画板或图片交付。
---

# 已有产品方案研究

默认使用中文。把这一步当作产品方案与交互设计之间的“证据层”：上游给出要解决的问题，本 skill 查清已有产品采用了哪些解法、为什么成立、在什么条件下失效，再把结论交给下游设计。

## 职责边界

### 接收上游输入

- 产品或功能需求、目标用户、使用场景、期望结果。
- 已确定的业务规则、平台、合规、技术、时间等约束。
- 可选：已知竞品、原型、历史方案或特别想验证的判断。

上游未提供但可以合理推断的信息，以“假设”标注后继续；会显著改变研究对象或比较维度的信息，先用一个简短问题确认。

### 只交付

- 已有产品的方案版图与代表案例。
- 每类方案的运行机制、适用条件、优点、缺点和代价。
- 对当前需求的 `可借鉴 / 不建议照抄 / 待验证 / 方向性建议`。
- 事实、推断、建议之间清晰的证据链和来源链接。

### 明确不做

- 不重新制定产品目标、功能范围、优先级或商业方案。
- 不替下游选择最终方案，不输出页面结构、控件组合、线框、交互细节或视觉规范。
- 不创建 Figma 画板、原型、实现代码或图片交付物。
- 不在聊天或报告中展示竞品截图；可以在研究过程中查看图片，但只内化为文字结论。

当用户同时要求研究和设计时，本 skill 只完成研究交接，并把设计部分留给对应的后续流程。

## 核心原则

1. **从问题出发，不从竞品名单出发。** 先拆用户任务、触发场景、决策点和约束，再寻找可比产品。
2. **研究完整解法，不做功能点罗列。** 关注入口、流程、信息、反馈、异常、控制权和代价怎样共同工作。
3. **按解法组织，不按工具或来源组织。** Mobbin、官网、文档、应用商店和社区证据必须归入同一分析框架，禁止建立“Mobbin 参考”“Agent Reach 参考”等结论类别。
4. **观察与建议分开。** 看到的事实、基于事实的推断、给当前项目的建议必须分别标注。
5. **借鉴机制，不复制表象。** 只有说明适用前提，借鉴意见才成立。
6. **证据不足就降级结论。** 不用猜测填满报告，不为凑数量保留弱相关案例。

## Step 0：能力与来源检查

开始研究前进行最小检查：

1. 确认 Mobbin MCP 可调用，优先检查 `search_screens` 与 `search_flows`。
2. 完整读取 `agent-reach` skill，并运行 `agent-reach doctor --json`；按 `active_backend` 选择渠道和命令。需要网页、社交、视频或开发者证据时，再读取对应的 `references/*.md`。
3. 检查工作区是否已有同一主题的研究记录或来源清单；复用有效证据，按具体 URL 去重，再补充新发现。
4. 记录研究日期、不可用来源、登录墙、地区限制、付费墙和时效风险。

Mobbin 或 Agent Reach 不可用时，不得假装完成双来源研究。说明缺失会影响什么；能在当前权限内修复则修复，否则询问用户是否接受降级研究。降级结果必须显著标注 `证据覆盖不足`。

不要求 Figma、图片生成或本地图片合成能力，因为本 skill 不产出视觉稿。

## Step 1：把需求转成研究简报

先将输入压缩为以下内部简报；信息充分时直接继续，不设置不必要的确认门槛：

- **用户任务**：用户最终想完成什么，而不是准备使用哪个功能。
- **触发与场景**：何时发生、频率如何、是否时间敏感。
- **关键对象**：用户需要查看、选择、创建、比较或修改什么。
- **关键决策**：哪里最容易犹豫、犯错、失去信任或放弃。
- **系统边界**：平台、数据、权限、合规、商业和技术约束。
- **成功信号**：什么变化说明解法有效。
- **研究问题**：本轮必须回答的 3–6 个问题。

不要擅自补写上游产品方案。若需求本身包含未经验证的解决方案，把它改写为待验证问题。例如，将“需要做一个分步弹窗”改写为“已有产品如何帮助首次用户完成高认知负担任务，并控制中断成本”。

## Step 2：建立可比对象与比较维度

按以下层级寻找对象：

1. **直接同类**：相同用户、相同任务、相近业务约束。
2. **任务同类**：行业不同，但用户要完成的工作相同。
3. **机制同类**：采用相近的决策、引导、协作、反馈或容错机制。
4. **约束同类**：面对相近的高风险、低频、权限、隐私或复杂数据约束。
5. **反面案例**：能说明某种做法为什么失效、被投诉或被放弃的案例。

默认比较以下维度，只保留与当前需求相关的项：

- 入口与触发时机
- 核心任务路径与步骤负担
- 信息架构与信息渐进披露
- 决策模型、默认值与用户控制权
- 反馈、确认、成功、空、错和恢复状态
- 信任、解释、权限、隐私与风险控制
- 新手与熟练用户的效率差异
- 跨端、多人或上下游系统衔接
- 商业收益与用户价值之间的权衡

先确定研究问题和比较维度，再搜索产品。不得因为搜索结果容易获得就改变问题。

## Step 3：使用 Mobbin 研究产品行为

Mobbin 用于确认真实产品界面和流程怎样承载方案：

- 页面、组件、状态问题使用 `search_screens`。
- 多步骤任务、前后状态与异常路径使用 `search_flows`。
- 根据可迁移性同时搜索 Web 与 iOS；不要按设备类型拆分最终结论。
- 组合搜索：产品名、用户任务、页面类型、状态、交互模式和同义词。
- 对每个重要研究问题运行多组聚焦查询，并在结果仍有新增价值时继续翻页。
- 必须查看实际 screen/flow 后再描述；不得仅凭标题、标签或元数据推断界面内容。
- 保留具体 `mobbin_url`、产品名、页面或流程名、平台和访问日期。

Mobbin 的作用是提供“产品实际上怎么做”的证据，不等于证明用户喜欢、业务有效或方案适合当前项目。后几类判断需要 Agent Reach 的官方资料、评测和用户反馈补足。

## Step 4：使用 Agent Reach 补全公开证据

根据研究问题选择渠道，不机械遍历所有平台：

- **官方产品页、帮助中心、文档、更新日志、应用商店**：确认功能机制、目标用户、限制和当前状态。
- **独立评测、案例研究、行业文章**：理解方案背景、迁移条件和公开结果。
- **社区与社交平台**：寻找真实用户语言、困惑、投诉、变通方式和弃用原因。
- **视频或演示**：当流程时序、动态反馈或跨页面行为对判断重要时使用。
- **开发者资料**：当集成、权限、数据或技术边界决定体验时使用。

遵循 Agent Reach 的路由和重试规则。较大的多来源研究结束后执行 `agent-reach check-update`，但不要中断当前任务升级。

来源优先级：

1. 当前可验证的一手产品证据。
2. 能说明实际使用情况的高质量二手证据。
3. 有具体场景的用户反馈。
4. 仅作线索、不能单独支撑结论的聚合页或营销表述。

对会随版本变化的产品事实，优先使用最新来源并记录访问日期。官方营销文案只能证明厂商如何定位，不能单独证明体验效果。

## Step 5：建立证据账本

研究过程中维护结构化记录。每条关键证据至少包含：

| 字段 | 内容 |
|---|---|
| 产品 / 来源 | 产品名与具体页面、帖子或材料标题 |
| 方案类别 | 该证据支持的已有方案类型 |
| 观察事实 | 实际看到或读到的内容，不混入评价 |
| 来源类型 | Mobbin、官方、文档、应用商店、评测、社区、视频等 |
| 具体 URL | 可直接回到证据的链接；Mobbin 必须为具体 `mobbin_url` |
| 日期 | 发布或更新日期（若可得）与访问日期 |
| 相关性 | 它回答了哪个研究问题 |
| 可信度 | 高 / 中 / 低，并说明限制 |

使用以下标签区分语句：

- `[事实]`：可由来源直接验证。
- `[推断]`：多个事实支持的解释，说明推断链。
- `[建议]`：结合当前需求和约束后的方向性判断。

同一重要结论尽量获得不同类型证据的交叉支持。若只有单一证据，降低可信度并说明原因。

## Step 6：归纳现有方案类型

按“解法如何工作”将案例聚合为 2–5 类有区分度的方案。每类只写一次综合结论，避免逐产品重复评价：

### 方案类型名称

- **运行机制**：它如何帮助用户完成任务。
- **代表产品**：列出最能说明机制的真实案例与链接。
- **适用条件**：什么用户、频率、风险和约束下成立。
- **优势**：减少了什么成本，提升了什么确定性。
- **缺点与代价**：把复杂度、风险或成本转移到了哪里。
- **失败条件**：什么情况下会误导、打扰、降低效率或难以实现。
- **证据强度**：强 / 中 / 弱，以及尚缺什么证据。

方案类型应互相可比较。例如可以按“系统替用户决定 / 系统推荐、用户确认 / 用户完全控制”分类；不要按“App / Web / Mobbin / 官网”分类。

## Step 7：转译为后续设计指导

对每类方案，结合当前需求输出一个简短四段式判断：

- **可借鉴**：值得采用的机制或原则，以及成立前提。
- **不建议照抄**：不适合复制的做法、原因和缺失前提。
- **当前需求机会**：当前项目可以重点解决的矛盾或体验目标。
- **待验证**：需要产品、数据、技术、合规或用户研究补充的问题。

“方向性建议”只描述设计目标、优先级、约束和判断标准，不规定具体页面或控件。例如：

- 合格：`优先让用户在承诺前理解影响范围，并保留可恢复路径。`
- 越界：`使用三步抽屉，第二步放单选卡片，底部固定主按钮。`

若某个具体形态是现有产品方案的重要组成，可以作为 `[事实]` 描述；不得直接把它写成当前项目必须采用的方案。

## 最终输出模板

最终报告保持可被后续 skill 直接消费，默认使用以下结构：

```markdown
# [需求主题] 已有产品方案研究

## 1. 研究结论摘要
- 3–6 条最重要结论，标注证据强弱。

## 2. 当前需求与研究问题
- 用户任务、场景、约束、关键决策和本轮不覆盖内容。

## 3. 现有方案版图
| 方案类型 | 代表产品 | 核心机制 | 适用条件 | 主要代价 | 证据强度 |

## 4. 各类方案分析
### 4.1 [方案类型]
- 运行机制
- 代表案例与来源
- 优点
- 缺点 / 代价
- 适用与失败条件

## 5. 对当前需求的指导
### 可借鉴
### 不建议照抄
### 当前需求机会
### 待验证问题

## 6. 给后续设计流程的交接
- 必须保留的用户目标与约束
- 建议优先探索的方向
- 应避免的模式
- 后续方案需要回答的关键问题
- 可用于评审方案的判断标准

## 7. 来源与证据限制
- 按方案类别列出具体链接、访问日期、可信度和已知缺口。
```

不在最终输出中嵌入、拼贴或附件形式展示图片。来源链接可以保留，便于后续核查。

## 完成前质量门槛

交付前逐项检查：

1. 每个研究问题都有结论，或明确写出证据缺口。
2. 同时覆盖直接同类和至少一种相邻可比对象；若市场过窄，说明已穷尽的查询与限制。
3. 每类方案至少有一个真实产品证据，不用概念或生成内容冒充案例。
4. 关键产品行为已查看实际界面、流程或官方材料，未仅凭搜索摘要下结论。
5. 重要链接可追溯；Mobbin 结论包含具体 `mobbin_url`。
6. 明确区分事实、推断与建议，没有把相关性写成因果性。
7. 每条“可借鉴”都写明前提，每条“不建议照抄”都写明原因。
8. 输出按方案机制而非来源、平台或设备分组。
9. 没有输出图片、设计稿、Figma、控件规格或交互实现。
10. 给后续流程的内容足够具体，能够用于比较方案，但没有替后续流程做方案决策。

任一关键门槛不满足时，继续补证据；若因权限、来源或市场稀缺无法补齐，诚实标记 `Partial evidence`，列出已尝试的渠道、查询和需要用户协助的具体事项。

## 禁止事项

- 不虚构产品、功能、截图、URL、用户评价、效果数据或 Mobbin 结果。
- 不把营销声明、单条评论或搜索摘要包装成已验证结论。
- 不为凑案例数量保留低相关证据。
- 不把竞品普遍采用等同于适合当前需求。
- 不输出“行业最佳实践”而不说明证据和适用条件。
- 不用工具名、来源名或设备类型代替方案分类。
- 不把研究结论扩写成 PRD、线框、交互说明或视觉方案。
