# 旧版设计规范（Shopee EDS）

Shopee EDS 时期的设计规范文档，从原 `旧前端组件源码/eds/docs/zh/` 提取，2026-08-09 整理保留。前端源码（Vue/JS/TS/SCSS）、SVG 图标和字体文件已删除——当前工作方式是 **Figma-only**，那些的真源都在 Figma。

## 已接入 L1–L3 的部分（出图时读 L1–L3，不读本目录）

| 本目录文档 | 接入到 |
|---|---|
| `number.md` | [`../../L3_全局规范/数字与日期规范.md`](../../L3_全局规范/数字与日期规范.md) —— 填上了 L3 原本标注「待补」的那条 |
| `motion.md` | [`../../L3_全局规范/全局规范.md`](../../L3_全局规范/全局规范.md) §八之二 —— 并修正了通用视觉规范里错误的 150–300ms |
| `keyboard.md` / `mouse.md` | [`../../L3_全局规范/键鼠交互规范.md`](../../L3_全局规范/键鼠交互规范.md) —— L1–L5 此前完全没有这一层 |

上表三项**以 L1–L3 为准**。本目录原文只在需要查证细节时打开（例如 `number.md` 的 §2.4–2.8：时间跨度、常用时间段、相对时间、时区、电话账号邮编）。

## 尚未接入、需要时自行查阅

`adaptation`（适配与画板尺寸）、`colors`（颜色描述规则）、`layout`（布局）、`typeface`（字体）、`text`（文本）、`structure`（结构）、`logo`、`design-value`、`introduction`。

其中 `colors` / `typeface` 与 L1 的 Token 体系**大概率冲突**——L1 是从当前 Figma 抄的，是真源；本目录是 EDS 旧库时期的。冲突一律以 L1 和当前 Figma 为准。

## 组件设计说明（`组件/` 目录，40 份）

各组件的设计意图、使用场景和反例。L2 的 usage 文档里有 26 份仍是早期 React props 版、缺设计意图，这 40 份可以用来补。

⚠️ 属性表、变体名和数值**一律不可信**——那是 EDS 旧库的，当前真源是 Figma library。只取"什么时候用、为什么这么用、什么时候不用"。

## 边界

- 本目录是**历史参考**，不是规范真源。
- 与 L1–L5 或当前 Figma 冲突时，一律以后者为准。
- 日常出图**不读本目录**。只有 L1–L5 留白、需要查证历史依据时才打开。
